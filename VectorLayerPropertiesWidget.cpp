#include "VectorLayerPropertiesWidget.h"

VectorLayerPropertiesWidget::VectorLayerPropertiesWidget(QgsMapLayer* layer, QWidget* parent)
    : LayerPropertiesWidget(layer, parent)
{
    // 可以在这里添加矢量图层特有的一些配置
    // 例如，添加更多的属性列表项
}